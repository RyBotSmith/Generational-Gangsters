// commands/gambling.js — Slash command definition only. No game logic.
// Parse options → call service → send result.
// TODO: implement in feature session

module.exports = {
  data: null,
  async execute(interaction) {},
};
